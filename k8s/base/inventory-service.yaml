apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: cloudmart
  labels:
    app: inventory-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
        version: v1
    spec:
      containers:
        - name: inventory-service
          image: cloudmart/inventory-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: cloudmart-config
          env:
            - name: PORT
              value: "8085"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: cloudmart
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8085
      targetPort: 8085
      name: http
