apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: cloudmart
  labels:
    app: payment-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        version: v1
    spec:
      containers:
        - name: payment-service
          image: cloudmart/payment-service:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: cloudmart-config
          env:
            - name: PORT
              value: "8084"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: cloudmart
spec:
  selector:
    app: payment-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
